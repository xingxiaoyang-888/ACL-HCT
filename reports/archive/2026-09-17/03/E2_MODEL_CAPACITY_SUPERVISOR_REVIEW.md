> 历史快照：以下状态、建议和授权只代表原文写作时点，不是当前执行指令。当前结论见 [报告总览](../../../README.md)。原始字节另存于归档 ZIP。

# E2无图模型能力对照：主管审核

研究问题：同一文本、监督数据和有界训练预算下，原双曲GNN是否具有相对无图文本MLP的完整valid检索优势？当前尚无正式MLP结果，竞争力证据不足；本次源码与CPU工程准备通过，不能代替科学结论。

2026-09-17固定源码e1dd5ee3e9b2ac2a89cdf6b49dd71247987e4246，配置canonical SHA c40ad1d32bb338047f307749bf0ef023c204ce1cf39550e003d77ce665df7634。主管独立核对60个源码/测试/scripts哈希与Git对象及工作文件一致，38个既有源码/配置与确认4960901版本逐字节一致。16项无Git工程测试通过；最终JSON安全缓存检查修改后受影响的1项重测通过，保留前后证据，没有重跑未受影响测试。

逐段审核新增模型、欧氏缓存排名、训练与测试源码，核对全候选过滤/目标恢复/精确计算同分、固定CPU批次流与四负例、首次完整valid最高micro选模、partial或非有限停止、真实模型/优化器/RNG保存、配置及原数据/基线绑定、科学CLI失败退出。无图模型只编码出现于本批查询的实体，因编码器没有跨实体运算，其损失/梯度与全编码一致；已有FP64必要测试直接验证。原GNN和MLP同为82,433参数仅为描述，不据此声称单因素消融或相同计算量。

放行范围：此固定版本自己的单L40合成CUDA门槛，40节点/128维、4训练步及4完整合成valid，1600对直接/缓存评分误差上限1e-5；2CPU/16GiB、外10分钟、内120秒。遵守全部ACL共享最多4并发，等待原确认链释放资源后运行，不占用CVPR预留资源。实验任务交回原始quality、run、best/last及成本来源后，由主管独立复算、核对保存权重。门槛通过后才能交接原固定两seed正式有界训练，不改变已在运行的确认。

正式结果只逐seed描述，不以两seed作训练随机性显著性证明。文本模型若更强则保留负结果，不自动调参、延长预算或重训GNN；即使更弱，也仅支持此数据/预算/参照内的优势。确认检验继续针对原冻结模型，不因这个参照结果删除确认条件。

## 新CUDA验收与正式训练交接

主管独立核对31源码、原quality/run公共副本字节、两个checkpoint原SHA及逐参数SHA、优化器与CPU/CUDA RNG、四次完整synthetic排名及首次best。用保存的best权重、独立重建的PyTorch MLP与原合成特征在CPU复现全部7条选定排名，1600对直接/缓存评分差5.96e-8，通过固定1e-5门槛。新增quality raw SHA a287ac03251e41b539e572f32d43ea2f03691c0be9c7e027e1e41c00a8d0e71e。没有重新训练。

放行原固定seed11/23各1024步、四次完整valid的有界能力对照；两片依赖串联使用释放的一个ACL槽，保证与尚在运行的确认合计不超过4卡。源码、数据、监督、选择规则和预算均不改变。两片完成后再独立评估模型能力与科学目的，失败按既定规则保留并分析，不用后验加训追求有利结果。
