> 历史快照：以下状态、建议和授权只代表原文写作时点，不是当前执行指令。当前结论见 [报告总览](../../../README.md)。原始字节另存于归档 ZIP。

# E1 最小闭环：质量补丁与单次运行交付

**状态：用户于2026-09-16明确说“继续补充E1实验”，已授权既定102条件的一次有界运行；最终质量检查后由主管交实验任务执行。代码任务未运行该科学清单，E1阶段尚未验收。** 每个关键实验仍须完成质量和预定指标审查、取得用户明确同意后才执行。源码发布、主管就绪通知、单测或空闲资源都不是运行授权。协议见 [E1_MINIMUM_CLOSURE.md](../../../../docs/operations/E1_MINIMUM_CLOSURE.md)，批准规则见 [EXPERIMENT_APPROVAL_GATE.md](../../../../docs/operations/EXPERIMENT_APPROVAL_GATE.md)。E2/E3新增工作保持暂停，已写N1文件保留且不混入此提交。

## 配置与静态成本

`configs/e1_minimum_closure_proposal.json` 按主管提案逐项展开，保留历史 `proposal_pending_explicit_user_approval` 元数据；当前真实授权由独立批准记录绑定此配置SHA与最终源码版本。只执行过组合数/参数合法性计算，没有运行这些总体或观测任何新增科学结果。

| 块 | 条件数 | exact子集数 |
|---|---:|---:|
| 原54同条件质量补全及新对照 | 54 | 138483 |
| 曲率0.25/4 | 12 | 80580 |
| 维度8 | 6 | 40290 |
| 等距移动0.4 | 6 | 40290 |
| N=8/32、k=3 | 6 | 15048 |
| seed23 | 6 | 40290 |
| seed23局部尺度 | 12 | 80580 |
| 合计 | 102 | 435561 |

新增48条件合计297078子集；单条件最多12870，全部<=20000，未转MC或增加条件。两遍原聚合计算共871122个子集实例，oracle产生871122个带符号输出；每个子集有三档缩放输出（恒等档直接复用）。质量审查发现历史三修正缺少新数值残差字段，因此本次已授权范围包括102条件的全部原方法均重新计算，三个修正合计1306683个子集方法评估，其中相同历史54的额外质量补全为415449次。等距块另算未移动的四种原方法，共161160次，以逐点核验。这些计数不等同于相同成本的kernel调用，尚无补实验实测时长；本次授权预算为CPU两线程/8GiB/外层最多30分钟，不自动重试或追加。

## 控制算子与科学边界

`e1_controls.py` 固定 `alpha=1+lambda*(1/k−1/N)`，lambda=-1/0/+1；以坐标原点作 `Exp_o(alpha Log_o(Y_none))`。不拟合系数、不使用总体p或预测偏差；lambda0或k=N直接返回原输出，k1/2仍按缩放定义执行。越域记录整方法失败且不输出部分样本均值，不裁系数或重抽。原点缩放不具一般等距等变性，报告明确排除它的等变性要求。

精确oracle先遍历全部子集固定μ=E Log_p(Y)，再按完全相同索引流生成±(ξ−μ)。两遍索引SHA和原偏移向量和逐项核对。记录原残差中心化协方差与输入e的协方差（分母M），以及Exp/Log后正负输出的协方差（2M个等权符号样本）；两者相等的权重定义明确，不把2M当独立MC次数。输出完整ambient矩阵、Lorentz迹、固定径向与横向方差差、中心化均值、流形/切向残差、Log/Exp往返数值代理。原点与p重合时径向未定义，保留null。

零切向均值、oracle MSE=原方差以及MSE差=−bias²是构造恒等关系，只用于核验实现，不是新发现、可部署改进或实际层级损伤证据。该oracle不替代独立校准bank和真实图跨节点/传播N1。

新增条件同时记录none、third_unclipped、third_protected、jackknife_protected及三档缩放/oracle；原修正公式不变。等距块用同子集逐输出和Log向量核对Lorentz boost对应。记录实际局部距离、sqrt(c)缩放距离与切向矩阵秩；有限尺度误差斜率仅作描述，低于记录的roundtrip代理或缺数值记录时给NA，不宣称渐近定理。固定spread的曲率块与局部无量纲尺度有耦合，N块总体构型也变化，按协议保留这些限制。

## 历史来源与执行入口

`e1_closure.py` 默认仅做静态清单：

```bash
PYTHONPATH=src python -m acl_hct.e1_closure --config configs/e1_minimum_closure_proposal.json
```

真正计算必须显式 `--execute`，并提供 `--approval-record`、固定 `--source-commit`、`--historical-result` 和全新 `.json` 输出。没有批准记录时在任何科学计算前拒绝。批准记录须对应真实用户消息、准确的config SHA和release、质量/入口指标已通过、预先定义的验收标准、CPU两线程及时限。该文件是审计记录，不是用户身份的密码学证明，执行者仍须核实用户实际同意；不可用合成测试记录代替真实批准。默认内部软时限1700秒、外层最多1800秒，在case边界检查，单case可超过剩余时间，外层负责硬上限。入口不提交作业、不重试、不选参数、不推进后续阶段。

旧结果核验来源6cd0caaa6a970b6f472ff45e58a1b3e65ea57e06及规范LF SHA256 `9c3b26297c1667c16fa76ac5dcf306c5a055a2f2fbdbb8d24355e86ad1fd15e1`，保留实际读取raw hash。原54的none/三个修正本次均重新生成当前指标及数值残差，逐项比较旧均值/MSE/索引流；新增显式裁剪/回退计数，旧记录只有比例时在同一分母下严格核对比例。旧artifact仅作为独立参照，没有任何旧科学值复制进当前method结果或用新残差倒填旧记录。不覆盖原JSON，不改写“bias降低但MSE升”及jackknife有时更好的历史结果。新增输出为schema3，记录执行源码hash及批准/配置来源。

配置策略为 `recompute_same_methods_for_numerical_audit_compare_immutable_artifact`，历史块已改名 `historical_quality_completion`。规范配置SHA256现为 `d849d11425f30243f28afe9a1bbeccc4a8c59b55aaeb41ad7bb98aae755d214f`；旧配置/批准记录不能静默适用于扩大后的质量补全范围。用户本轮明确授权覆盖此质量补全范围；运行批准记录仍须绑定本SHA和最终固定版本。代码任务未运行科学清单。

绘图按实际method keys包含全部新增对照，分组加入N和seed，避免将不同总体连成同一组。旧格式仍可读取，未修改原图或正式结果。

## 数值审计与覆盖率补丁

按主管 [预定验收提案](../../../../docs/operations/E1_CLOSURE_ACCEPTANCE_PROPOSAL.md) 新增只读 `audit_results(config, report['cases'])`，不生成总体、不修改输入字典。每个计划case及8个方法都列出处理/未执行/输入失败/方法失败/缺结果状态，以及独立的数值通过、失败或证据不足。`planned_cases_processed` 只表示循环处理数，不表示质量或科学通过。重复、替换、漏项、缺字段、非有限值和计数/hash不一致均不会默认通过；输入失败及超时会列出具体case/control，`missing_controls` 不再硬编码为空。

固定数值门槛：两遍和差、流形/切向/LogExp残差、oracle零均值与协方差/径向横向/MSE恒等差<=1e-10；历史四种原方法均值/MSE差<=1e-12；variance/MSE最低允许-1e-12并保留原值；boost逐点与向量差<=1e-9。lambda0及full缩放直接复用输出，新增实际比较样本数、逐输出最大差和bitwise一致标记，严格要求全部样本、零差及true标记。径向NA必须有false定义标记，单独计数，不填零；其他缺数值字段标证据不足。历史裁剪/回退使用相同分母，计数或比例须一致。

输出分别给完成性、各方法数值可用性、对照缺口和数值汇总；全部这些机器检查通过仍将整体质量置为待独立来源核验。源码/批准/历史artifact哈希由入口先校验，并仍需主管对固定归档/原始结果做独立来源复核；此只读数值函数不冒充该复核。`scientific_effect_status=pending`、`E1_passed=false`、`next_experiment_authorized=false` 始终保留，不能用机器数值通过自动批准E1或E2。

## 工程测试与交付

初版9项单邻域控制测试通过；加执行器后13项通过/16.36秒。最终将全部新增源码、两份目标测试、待批配置及绘图脚本覆盖到真正无Git的临时archive，以该目录为cwd运行：

```bash
python -m pytest tests/test_e1_controls.py tests/test_e1_closure.py tests/test_e1_archive.py -q
```

结果 **16 passed /25.09秒**。仅做5/6点（及历史archive测试的4点）代数fixture、只读历史hash与静态清单；没有执行102条件。覆盖固定缩放独立参考/full恒等/低k非回退、完整协方差与径向横向矩、两遍hash、chunk一致、原方法负结果、逐输出/切向等距对照、越域保留、批准缺失/来源预算不符拒绝、历史文件不变、无Git实际CLI静态清单/拒绝执行，以及全部方法进入绘图。

随后仅补充原54条件的静态身份集合与已验证历史artifact精确对应断言，该只读测试单独通过（1 passed /5.04秒）；没有重算历史科学结果。

本次数值审计补丁仅针对受影响范围：新增29项小fixture/人工结果字典边界测试，加3项相关旧测试，32 passed /5.82秒。测试包含每个FP64阈值恰好取界及其外侧下一个可表示浮点值、缺字段、NaN、完整覆盖、输入/方法失败、超时未执行、重复/替换case、径向NA、lambda0/full实际输出核对、历史各方法差与裁剪/回退记录。测试只生成四个6点工程小例，静态历史文件仍只读；未运行102总体或新远程作业。没有重跑既有全部suite，也没有降低阈值。

主管已审阅实现与指标口径、历史重算比较、失败覆盖、固定阈值及范围；待最终固定归档检查交付后，由主管完成来源核验并转交实验任务执行本次用户已授权的单次运行。当前没有新的效应量、科学通过判定或E1完成声明；E2仍未授权。

最终质量补丁在全新临时 `git archive HEAD` 导出并覆盖最终源/测试/配置的目录中复核：目录无 `.git`，`git rev-parse --show-toplevel` 返回非零。仅运行上述29项新增及3项受影响测试，**32 passed /7.11秒**。该检查覆盖最后的缺字段稳健性变更，不重复全套旧检查。归档测试范围及文件规范LF哈希见 [e1-closure-quality-tests.json](../../../e1-closure-quality-tests.json)；102条件科学运行仍由主管转交实验任务。
