> 历史快照：以下状态、建议和授权只代表原文写作时点，不是当前执行指令。当前结论见 [报告总览](../../../README.md)。原始字节另存于归档 ZIP。

# E2多预算开发校准：运行前审查

2026-09-17。研究问题：减少邻居采样量是否同时改变局部/两层几何、已有祖先顺序及父检索表现？本批尚无真实科学结果，研究目的为**证据不足**；工程准备已通过下述审查。用户已授权“继续进行，不要自停E2”，按同一E2范围依次完成GPU质量门槛和固定开发预算。

## 固定版本与审查结果

代码4a0fdb1c66512c247def473b553dd59ced690210，配置规范SHA9476fd2cd6dd1527f22ba2538f4f2c29d8adc847ef6df74d47553fa1256e6ff7。主管逐文件比对最终无Git测试记录中六个文件的规范LF SHA与Git对象及工作文件，一致；checkpoint身份与数值门槛和原入口配置相同。必要针对性测试13项通过、无跳过，22.80秒，含受影响旧入口检查。

源码审查覆盖：共享采样计划与独立层子流、局部A外零偏移和真实传播保留的区别、共同完整基点、固定根与加权covered分母、全valid原生表示排名、MC整图重复单位、低维逐次记录与无损数组归档，以及途中失败/超时停止。审查发现的CLI失败仍退出0问题已修复，failed/incomplete保存结果后退出2，并加入对应测试。

质量通过不表示研究假设成立。13项CPU工程测试也不能替代真实CUDA验收或真实网络证据。

## 执行交接

先执行单L40、2CPU/16GiB的40节点CUDA工程fixture，fanout4/16各4次、任务1次；内部120秒、外层10分钟。固定源码归档及上传逐字节核验，输出与源码对应后主管独立核对。该小样仅验新增路径，不估算82115节点全图成本。

CUDA验收通过后，先运行seed11/23各fanout4的完整开发分片，记录真实成本。全部设计固定为两个seed×fanout4/8/16/32/64，共10片；每片128次四路径配对、前8次S/S完整valid，最多2卡并行，每片内部3300秒/外层60分钟。实际质量与成本允许后继续其余预定片，不按研究结果选择有利预算，不增加重复直到显著。

新增范围仍为E2开发校准；确认池不评价、test关系不开启，不进入E3/N1/修正/重训。失败保留并修复工程原因后继续，科学负结果如实报告。来源、原始记录和测量成本以执行报告为准。

## CUDA实测验收及首批交接

40节点单L40小样已完成。主管以不导入模型代码的独立NumPy脚本复核18个源码SHA、3个NPZ及索引、176个数组身份；224项已记录数值检查重新对照原阈值，通过。逐次记录重算各组几何与结构均值/MC标准误，并从完整排名重算任务指标，一致。质量报告raw SHA为4a392b7c955461b09670af2f4f35425d4ba7e7fba58140bd7c46cf002590bcd2。

GPU工程门槛通过，科学解释仍不适用于真实模型。已交接同一固定源码下seed11/23两个f4完整开发分片，每片128/8，原资源边界不变。实际运行状态和成本待执行记录，不以本条交接等同已提交或完成。
