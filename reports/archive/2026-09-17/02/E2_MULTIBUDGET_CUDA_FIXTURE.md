> 历史快照：以下状态、建议和授权只代表原文写作时点，不是当前执行指令。当前结论见 [报告总览](../../../README.md)。原始字节另存于归档 ZIP。

# E2 多预算 CUDA 工程验证

研究问题：真实模型减少邻居采样量是否伴随结构或父关系检索损伤？本次仅验证新增实现能否在 CUDA 上按固定质量门槛运行，尚无真实模型科学结论。工程验证目的已达到；研究目的仍证据不足，因为使用的是 40 节点工程小图。

固定源码 `4a0fdb1c66512c247def473b553dd59ced690210`，单张 NVIDIA L40、2 CPU、16 GiB。fanout 4/16 各完成四次几何重复及一次完整任务重复，入口和新增路径检查通过，程序正常退出。没有启动真实数据分片。

归档逐文件与固定 Git blob 字节一致；报告中的 18 个源码哈希全部匹配。回传后独立以 NumPy 核验入口及两份预算归档，共 176 个数组的完整清单、dtype、shape、数据 SHA256，以及 JSON 索引和 NPZ 文件 SHA256，全部一致。原始报告保持字节不变，内嵌 pending_supervisor_review 状态未修改。

程序墙钟 5.59 秒，内部测量 1.769 秒，最大进程 RSS 1,132,472 KiB；调度记账分配时间 5 秒，作业完成后资源已释放。这些工程小图成本不能外推至 82,115 节点真实图。

证据：[原始 CUDA 报告](../../../e2-multibudget-cuda-quality.json)、[核验和成本记录](../../../e2-multibudget-cuda-metadata.json)。原始报告 SHA256 为 `4a392b7c955461b09670af2f4f35425d4ba7e7fba58140bd7c46cf002590bcd2`。全部 NPZ、原索引及调度日志在本地私有证据目录保存。

下一步按既定交接完成主管独立质量验收，再运行 seed11/23 的两个真实 fanout 4 分片测量成本；保持每片 128 次几何重复、前 8 次 S/S 完整 valid，以及原检查点、数值门槛和时间边界。
