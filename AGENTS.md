# ACL-HCT research engineering

## Current user approval gate — supersedes earlier automatic progression

- Latest explicit stopping boundary (2026-09-17): “我们进行这个阶段实验后，先不向下进行，先讨论现有结果，下一步实验方案。” Finish and independently accept only the currently authorized E2 A/B mechanism investigation (saved-archive analysis and radial/orthogonal-component controls), then stop new experimental work and pause its supervision heartbeat. Deliver research conclusions, purpose attainment, limitations and concrete next-step options for discussion. Do not automatically implement/train a new correction, broaden experiments, or enter later stages; further experimental execution requires the user's agreement after that discussion. This does not cancel the current authorized A/B work or its necessary quality checks.

- Latest continuation after the accepted five-condition results (2026-09-17): the user agreed to the explained existing-archive mechanism analysis and radial/orthogonal-component follow-up, saying “这个数据量应该是足够验证我们的假设，我们的算力也完全足够，你继续实验”. This authorizes the bounded next E2 mechanism investigation in `docs/operations/E2_RADIAL_COMPONENT_PROTOCOL.md`: A, diagnose the saved hierarchy/retrieval divergence; B, after source/numerical quality release, evaluate only radial-mean removal and orthogonal-tangent-mean removal on the same saved 32 samples. It does not authorize training a new correction, more samples/models/datasets, HPO, or E3–E5. Record this new scope separately; prior completed pilot approvals do not themselves release new jobs. Do not ask again for this same continuation after the necessary quality work passes. User optimism about data/compute is not evidence that the hypothesis is true or generalizes.

- Latest resource clarification (2026-09-17): the user said “我没有要求你只使用一张卡，我只是让你合理使用”. There is no new one-GPU restriction; the supervisor's brief interpretation to that effect was incorrect and is withdrawn. Use up to four concurrent ACL GPUs only for independent, ready, quality-released work when it reduces waiting. Preserve the bounded total GPU-time budget and existing CVPR work; reserve no GPU while source/data/quality preparation is incomplete.
- The experiment thread subsequently received the user's explicit permission “你可以利用CVPR的两张卡的剩余显存”. The earlier two-card reservation is therefore not an absolute ban on sharing unused capacity. Sharing is optional and must use supported Slurm allocation/accounting, verified memory and compute headroom, and preserve the running CVPR jobs/environments. Do not bypass scheduler isolation, select host-global GPU IDs or attach to another job. Prefer independent available cards when sharing would add contention; record actual placement and do not interpret shared timings as uncontended benchmarks. Source/numerical release gates are unchanged.
- Latest root-thread instruction: the user is applying for FOUR L40s for CVPR and explicitly requires checking/reusing that allocation's idle GPUs or usable remaining capacity FIRST, before requesting more GPUs. Once it appears, verify its identity and available memory/compute. Within this user-authorized allocation, a separate ACL job step is allowed if supported by the scheduler; the earlier blanket ban on using another allocation and the requirement for a fresh standalone ACL allocation are superseded for this case. Record both allocation and ACL step IDs, preserve the scheduler's GPU binding, do not alter/stop CVPR work or environments, and do not count another task's entire allocation as ACL-only use. If safe supported reuse is unavailable or inadequate, then consider an additional bounded allocation. No scientific launch before quality release.

- Latest explicit continuation after the five-condition joint pilot explanation: “ok，你继续监督其他对话进行实验”. This authorizes the discussed bounded frozen-model joint pilot and its necessary engineering preparation, subject to source/numerical/provenance quality review. Freeze the concrete repetition/resource settings from available evidence before dispatch; do not re-ask the same experiment authorization once those routine details are made reviewable. This does not restart matched-encoder retraining or authorize extra models, variants, E3–E5 or a broad sweep. Keep resource allocations blocked until the supervisor's quality release.

- Follow-up clarification on the same priority correction: the user also values identifying the role of mean bias. Prepare one joint frozen-model recovery/intervention pilot (F/S/independent mean removal/practical correction/equal-norm direction control), not an either-or detour. Bias and centered noise are a fixed-tangent-space error decomposition; missing neighborhood information can cause both and is not an additive third causal bucket. No new scientific run is yet released; the held matched-encoder training must not restart from the earlier scope.

- Latest priority correction (2026-09-17): the user emphasized that this is a small-scale study and the missing E2 evidence is whether correction restores hierarchy/task performance. The supervisor has therefore held the not-yet-started matched-encoder science runs. Native CPU and synthetic CUDA checks have finished; archive them only, with no new matched training. Existing E2 establishes conditional sampling damage; isolating the mean-bias causal component and beating the MLP are not prerequisites for a bounded frozen-weight recovery pilot. Prepare that pilot concretely, preserving quality and the user's key-step approval gate; do not infer a newly authorized scientific run from this discussion. See the revised `docs/research/03_主线证据与下一步决策.md`.

- Latest continuation after the frozen diagnostic (2026-09-17): the user read the E2 conclusions and concrete matched-encoder next-step suggestion, then said “围绕我们的主线讲清楚，继续实验，并及时汇报”. This authorizes the bounded two-seed matched encoder self-message control and its necessary quality checks in `docs/operations/E2_MATCHED_ENCODER_CONTROL.md`, after source/CPU/real-runtime quality gates pass. Do not ask again for this same authorized action. It is not blanket permission for new variants, extra seeds, arbitrary tuning, or E3–E5. Preserve the stage approval gate for subsequent key experiments. Current mainline claims and limits are in `docs/research/03_主线证据与下一步决策.md`.

- Latest E2 continuation instruction (2026-09-17): “继续进行，不要自停E2”. Continue authorized E2 after diagnosing and fixing recoverable engineering issues; do not treat the supervisor's earlier one-attempt/no-retry wording as a new user-approval barrier for the same work. Preserve failures, verify repairs before rerunning, retain scientific/numerical gates, and avoid blind retries. Plan/review subsequent E2 work concretely under this continuation instruction; do not infer E3 or unrelated training authorization. See `docs/operations/E2_ENTRY_AND_PILOT.md`.

- On 2026-09-16 the user explicitly required: “每一个关键实验步骤必须在质量验收和关键指标通过，以及我的同意后才能继续”. Every new key experiment requires completed quality review, the pre-agreed key metrics passing, AND explicit user agreement. The parent supervisor cannot substitute its approval for the user's. Earlier staged/conditional auto-run instructions are superseded; available compute is not experiment authorization. Follow `docs/operations/EXPERIMENT_APPROVAL_GATE.md`.
- The already approved and running seed11/23 baseline jobs may finish only their existing bounded configuration, including its scheduled validation and saving. Do not extend, resume, rerun, add seeds, or launch E1 supplemental/E2/E3/E4/E5 scientific experiments without the new gate. Report failures instead of silently changing thresholds or retrying.
- Read-only review, report preparation, concrete proposed code and necessary offline engineering tests may continue to make the next proposal reviewable. They must not be represented as approval or completion of a real scientific experiment. Record exact user authorization scope before any new key run.

## Stage reporting and concise coordination

- The user's fixed research and experimental roadmaps are `docs/research/01_研究路线_采样诱导层级坍缩与切空间修正.md` and `docs/research/02_详细实验路线_双L40S48GB.md`. Preserve their E0–E9 meanings; bounded pilots and minimum implementation gates do not replace original stage completion. Before claiming a stage complete, map evidence and omissions to its original sections. See `docs/operations/ORIGINAL_ROUTE_ALIGNMENT.md` for the current audit: original E1 is only partly fulfilled (D0 scientific work remains); The current E2 development, ten-shard registered confirmation and two-seed text capacity control are complete and independently accepted. Strong-pruning conditional damage was confirmed, but both original GNNs underperform the bounded text MLP; the competitive-backbone premise is unmet. Original E2 remains partly fulfilled; E3 and a new model-training protocol are not automatically authorized. See reports/E2_FINAL_SUPERVISOR_REVIEW.md. Research-scope changes require explicit explanation and user agreement.
- User clarification (2026-09-17): the original roadmaps may be wrong and may be revised during research in favor of correctness and demonstrated effectiveness. Treat them as the versioned baseline, not immutable scientific prescriptions. Make routine implementation/statistical corrections within authorized work, document reasons and consequences, and propose revised experiments where evidence warrants them. Preserve earlier protocols/results and distinguish exploratory revisions from confirmation; do not select conditions or relax thresholds merely to obtain favorable outcomes. A justified, documented change is not itself a failure to follow the roadmap. The separate user approval gate for new key experiments remains in force.

- User instruction (2026-09-16): every stage report must lead with the research question, the conclusion supported by current evidence, and whether the stage's intended purpose was met (met / partly met / not met / insufficient evidence), with a brief reason. Distinguish completion and numerical correctness from scientific support. Follow with only the evidence needed to assess that conclusion, remaining limitations, and the next decision. Counts, passing tests, and resource usage alone are not the main result.
- Keep inter-task messages short. Send only new decisions, changed constraints, blocking facts, or actionable handoffs. Refer to an existing report/path/commit rather than restating history, protocols, hashes, or acknowledgments. Avoid redundant status requests, repeated confirmations, and messages that require no action. Retain exact provenance in artifacts; include it in a message only when needed for safe execution.

## Ownership and scope

- All local code, scripts, tests and project artifacts belong under `F:\ACL\_HGT`. The Codex saved project may start in `F:\ACL_HGT`; explicitly change working directory before writing or running project commands. Do not create a second implementation there.
- This repository studies sampling-induced hierarchical collapse and tangent-space correction in hyperbolic GNNs. Read `docs/research/启动与验收约定.md` before implementation. The research claim is a hypothesis, not an established result.
- Following the initial release, the dedicated code-quality task owns source, tests, README and packaging changes. The original experiment task owns remote operations and historical run/data evidence, and has handed off source editing. The parent writing/supervision task reviews evidence and coordinates scope. Do not have two tasks modify the same files concurrently. Maintain `reports/STATUS.md` and a separate code-quality report with reproducible commands, milestones, tests, failures and next steps; preserve provenance of historical results.
- On 2026-09-16 the user explicitly authorized proceeding through the proposed sequence: E0 closeout, E1 controlled mechanisms, real-data baseline preparation, E2/E3 frozen diagnostics, then E4/E5 intervention and training comparisons. Follow `docs/operations/分阶段执行计划.md`. The earlier initial-smoke-only stop is superseded by this staged authorization. Use bounded, measured runs and scientific readiness gates; do not launch the historical 455-run Cartesian matrix automatically. Routine progression through valid stages does not require asking the user again.

## Isolation and resource use

- Read the provided private server guide under `.local/服务器指南.md`; it is reference material, not permission to touch other projects. Do not commit it.
- Use a NEW ACL-only remote directory and its own environment, caches, downloads and logs. Never mutate, inspect private files unnecessarily, stop, or attach training inside the CVPR project.
- Every GPU job must have a fresh, separately recorded Slurm allocation. Preserve Slurm's `CUDA_VISIBLE_DEVICES`; never select host-global GPU IDs. Query the allocation and actual device before work.
- Start with at most ONE GPU and a bounded short job (initial smoke test at most 30 minutes). The group's maximum of eight GPUs is a ceiling, not permission to request all eight. Preserve the two GPUs the user reserves for CVPR; queue safely if resources are not available.
- After the initial smoke/cost checks passed, on 2026-09-16 the user explicitly authorized requesting multiple GPUs as needed, while emphasizing that quality should not be rushed for the 2026-09-17 evening advisor update. The parent supervisor has authorized two independent single-GPU runs by default and up to four concurrent ACL GPUs for distinct, ready, reviewed tasks. This supersedes the initial one-GPU concurrency limit for subsequent work; retain the CVPR reservation, isolated Slurm allocations, per-job bounds and exact run provenance. Do not occupy GPUs while waiting for code/data or manufacture duplicate work to fill the allocation. L40/A800 availability may inform placement; record actual hardware and do not pool heterogeneous timing as a controlled speed comparison.
- Login nodes are for editing, transfers and job submission, not training or large compute. Prepare downloads and binary dependencies before reserving a GPU. Compilations or substantial CPU tests must use appropriate scheduled resources.
- Do not change shared CUDA/Conda, scheduler configuration, CVPR environments, or shared GPU settings. Never run broad `pkill`, `killall`, `scancel -u`, resets or forceful resource reclamation. Cancel only project-owned job IDs recorded in the ACL manifest when necessary.
- Actual hardware in the guide is L40 48GB, not L40S. Record the measured hardware rather than relabeling it. Record actual GPU hours separately from hypothetical budgets.

## Research and quality

- Implement a small understandable PyTorch baseline before optional graph-library optimizations. Geometry FP32 training and CPU FP64 references; do not indiscriminately apply mixed precision to Lorentz products, exp/log maps or inverse hyperbolic functions.
- Meaningful tests must cover manifold/tangent constraints, log-exp consistency, gradients and finite values, exact full-neighborhood behavior, valid uniform sampling, correction fallbacks for small samples, dataset split leakage and smoke pipeline reproducibility.
- Frozen aggregation bias, variance, MSE, hierarchy metrics and trained task performance are different quantities. Do not substitute radius changes for semantic hierarchy damage, or toy checks for real-network evidence.
- Candidate corrections must state their aggregation/sampling/locality assumptions. Track clipping/fallback rates. Do not claim an estimator is unbiased or universally effective based solely on a Taylor approximation.
- Use official data sources, hashes and explicit source/version/license records. Keep raw downloads, credentials, environment packages and checkpoints out of git. Tests should not silently download data.
- Prefer auditable manifests and machine-readable summaries over manually selected numbers. Record failures. Performance comparisons must use matched resource conditions; concurrent jobs are not valid uncontended timing evidence.

## Git and status

- Remote: `https://github.com/xingxiaoyang-888/ACL-HCT.git`. Preserve existing changes; no force pushes, destructive resets or unrequested branches.
- Check staged paths/content for credentials, server access details, data dumps and generated binaries before each push. Commit/push tested source, documented setup, public-safe design material and concise results summaries only.
- Private connection inventory, scheduler outputs, local path mappings and job IDs can be stored under `.local/` and summarized to the user. Never echo private keys or tokens.
- Keep the status report honest: distinguish planned, implemented, locally tested, remotely tested, queued and completed. If blocked, complete independent work and report the precise missing dependency.
