# E1 补充实验：预定验收口径与单次授权范围

状态：用户于2026-09-16明确说“继续补充E1实验”，授权本页既定102条件的一次有界运行；最终固定版本质量检查通过后由主管交实验任务执行。依据用户“质量验收、关键指标通过、本人同意”规则编写。配置保留历史proposal状态，实际批准由独立审计记录绑定固定配置SHA及源码。源码初次交付90d941180f9cf76210bc1597c92e6b35d3d9d965，后续仅质量检查补丁发布后以最终固定commit为准。

## 当前入口检查

- 实现已按固定缩放和exact总体中心化正负对照定义编写；主管已阅读实现、批准门槛及测试。
- 代码任务报告无Git归档下16项必要工程测试通过，另1项只读历史条件身份检查通过。测试仅4/5/6点小例和静态清单，不是102条件真实补实验。
- 静态配置102条件、435561个子集，最大单条件12870，全部可exact；历史54条件及其原始指标保持。
- 仍需在发布前核对本页数值口径对应的机器审计，以及失败/超时情况下的对照覆盖报告。审查已确认历史54条件三修正缺少部分数值残差记录；已授权方案将重算相同方法/条件补齐，并和不变的历史artifact比较，不能缺字段默认通过。用户授权不代替最终质量检查，也不覆盖E2或自动追加运行。

## 运行后的质量与入口指标

以下是固定FP64实现的一致性检查阈值，不是研究效应大小或“方法必须胜出”的阈值；不能运行后根据结果放宽。

1. 配置/源码/批准记录/历史artifact哈希一致；计划102条件必须逐一有结果或明确失败记录，不能漏项、替换构型、转MC。时间未完成时整体不通过完成性检查，等待用户决定如何处理。
2. 每个有效条件两遍子集索引hash严格相同，计数精确等于组合数；两个pass的原偏移和差绝对值<=1e-10。历史54的none及三种修正重新计算的平均向量、MSE与原记录绝对差<=1e-12，索引hash一致；裁剪/回退计数应一致，若原记录仅有比例则按同一分母核对。新数值残差仅属于本次运行，不回填历史记录。
3. 有效方法的数值字段有限，variance和MSE允许的负浮点残差不小于-1e-12，保留原始值不静默改零。统一流形/切向约束及Log/Exp往返最大残差<=1e-10。未定义径向方向允许NA但必须有标记和计数。
4. 缩放lambda0、k=N应与原输出一致；exact oracle的偏移均值范数<=1e-10，完整协方差逐项最大差、径向/横向方差差及MSE与原variance差均<=1e-10。这些是构造恒等式，不是新科学发现。
5. 等距块原聚合和原三种修正的逐输出、逐切向量boost对应最大绝对差<=1e-9。原点缩放明确不要求等变，不能将其不等变误判实现错误。
6. 任何输入/数值域/方法失败均单列，不输出选择性保留子集的科学均值；相应对照标为不可用，不能一律写missing_controls为空来表示所有对照已取得证据。completed只表示计划项被处理，不等于质量验收通过。

阈值超限先定位舍入、实现、几何域或理论定义问题；保留原结果。不以扩大阈值、改种子/尺度或忽略失败来追求通过。

## 科学结果如何判读

入口通过只允许报告这组实验的结果，不自动宣布候选有效或E1完成。另行逐块检查：对称/反向构型的一致性、原偏移与oracle方向/幅值、曲率与局部尺度耦合、维度/N/新构型依赖、局部有限尺度误差、简单缩放比较、保护裁剪以及bias/variance/MSE的相反变化。

小于记录的数值分辨代理的效应标记不可解析；代理不是严格误差上界。oracle零均值和协方差匹配不能当成“证明我们优于白噪声”的新实验发现。缺乏真实层级/任务证据仍须明确。

第一轮已经出现修正减bias但增MSE，方法收益尚未通过。补充实验的目的是确定机制和适用范围，不以候选胜出为完成条件。若结果仍不支持整体收益，主管须汇报不足及研究判断，用户决定下一步；不能把工程通过直接换成E2执行批准。

## 用户已授权的单次运行范围

仅运行E1_MINIMUM_CLOSURE.md对应固定102条件；CPU Slurm 2核、8GiB，内部1700秒、外层最多30分钟，0 GPU。全新输出，原结果不覆盖，不重抽、不改配置、不自动重试或追加条件。单case为最小软时间边界，外层调度提供硬上限。该时限是上限，不是预测用时。

用户批准仅覆盖这一次补充实验。结束后提交原始结果、来源和本页审计；下一关键实验仍须再次经过质量、预定指标和用户同意。
